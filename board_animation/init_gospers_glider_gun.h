#ifndef INIT_GOSPERS_GLIDER_GUN
#define INIT_GOSPERS_GLIDER_GUN

#include <stdio.h>
#include "init_board.h"

/**
 * @param board The board that will be initialized as empty
 * @return The board is a pointer, so no need for any return
 */
int init_gospers_glider_gun(int board[SIZE][SIZE]);

#endif